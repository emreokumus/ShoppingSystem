spring:
  application:
    name: shopping-card-service
  cloud:
    config:
      uri: http://localhost:8081 #config server uri
      #eureka server'a register olması için konfigürasyon ayarlarını config server'dan çekeriz.
server:
  port: 8084


